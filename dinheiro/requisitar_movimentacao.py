from dinheiro.movimentacao import adicionar, subtrair

def processar_adicao(saldo, valor):
    return adicionar(saldo, valor)


def processar_subtracao(saldo, valor):
    return subtrair(saldo, valor)