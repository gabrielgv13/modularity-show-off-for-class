from dinheiro.operacoes_saldo import adicionar, subtrair

def processar_adicao(saldo_atual, valor_informado):
    return adicionar(saldo_atual, valor_informado)


def processar_subtracao(saldo_atual, valor_informado):
    return subtrair(saldo_atual, valor_informado)